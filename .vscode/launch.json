{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "mcp dev server",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/.venv/bin/mcp",
            "args": ["dev", "server.py"],
            "justMyCode": true,
            "console": "integratedTerminal"
        }
    ]
}